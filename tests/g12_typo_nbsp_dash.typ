#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017.with(фича-неразрывные-величины: да)

// Узкий box: без неразрывного пробела тире уехало бы на отдельную строку
// (проверено: БЕЗ флага получаем 'тест' / '—' на разных строках).
// С флагом 'тест —' держится на одной строке (одинаковый yMin).
#box(width: 34pt)[
  тест —
]
