#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

= Раздел

// Подпись заведомо длинная — переносится на 3+ строки. Маркеры МЦЕЛЛ1/МЦЕЛЛ2
// — содержимое первой строки тела таблицы; проверяем, что последняя строка
// подписи не наезжает на них.
#таблица(table(
  columns: 2,
  [МЦЕЛЛ1], [МЦЕЛЛ2],
  [a], [b],
))[Очень длинная многословная подпись таблицы которая заведомо переносится не менее чем на три полные строки потому что содержит большое количество слов и продолжается дальше чтобы точно занять минимум три строки текста подписи целиком без сокращений]
