#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017.with(фича-маленький-отступ-вокруг-таблиц: да)

= Раздел

// Та же раскладка, что и в #1: шапка влезает внизу стр.1, первая
// строка данных СТРОКА1 уезжает на стр.2. Значит на стр.1 ни одной
// строки ДАННЫХ таблицы не было показано — следовательно «Продолжение
// таблицы 1» на стр.2 ложно (продолжать нечего).
#v(650pt)

#таблица(table(
  columns: 2,
  [Заголовок], [Значение],
  ..range(20).map(i => ([СТРОКА#(i+1)], [#(i*10)])).flatten()
))[Параметры]
