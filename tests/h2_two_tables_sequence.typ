#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

= Раздел

// Две таблицы подряд: ожидаем нумерацию «Таблица 1» и «Таблица 2», зазор
// между ними > 0 (не слипаются). Маркеры ПЕРВТАБ* / ВТОРТАБ* уникальны.
#таблица(table(
  columns: 2,
  [ПЕРВТАБкол], [Значение],
  [ПЕРВТАБа], [1],
  [ПЕРВТАБб], [2],
))[Первая]

Текст между двумя таблицами для разделения блоков.

#таблица(table(
  columns: 2,
  [ВТОРТАБкол], [Значение],
  [ВТОРТАБа], [3],
  [ВТОРТАБб], [4],
))[Вторая]
