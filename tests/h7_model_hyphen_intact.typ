#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017.with(фича-неразрывные-величины: да)

// Узкий box (34pt): БЕЗ флага название модели рвётся по дефису на 'ESP32-' /
// 'S2' (две строки — проверено компиляцией). С флагом дефис заменяется на
// неразрывный U+2011, поэтому 'ESP32‑S2' остаётся ОДНИМ токеном на одной
// строке и не разрывается. Проверяем целостность токена и одну строку.
#box(width: 34pt)[
  ппп ESP32-S2
]
