#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017.with(фича-неразрывные-величины: да)

// С флагом размеры вида '8x8x8' (латинская x между цифрами) преобразуются:
// каждая x заменяется на знак умножения × (U+00D7). Проверяем текстовый слой
// и координаты: × сохраняется в pdftotext, латинской x между цифрами не
// остаётся, и связка не разорвана (один box → одна строка).
Куб 8x8x8 вокселей и плоскость 16x16 пикселей.
