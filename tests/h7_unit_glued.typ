#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017.with(фича-неразрывные-величины: да)

// Узкий box (54pt): БЕЗ флага строка рвётся как 'ттт 100' / 'МГц' (число и
// единица на РАЗНЫХ строках — проверено компиляцией). С флагом связка
// «число+единица» заворачивается ЦЕЛИКОМ: 'ттт' остаётся выше, а '100 МГц'
// переносится вместе на ОДНУ строку. nbsp нормализуется poppler'ом →
// проверка по yMin (функциональная).
#box(width: 54pt)[
  ттт 100 МГц
]
