#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

// Длинная таблица переносится на 2+ страницы («Продолжение таблицы 1»).
// Это ОДНА таблица. #колво-таблиц должно быть 2 (одна длинная + одна
// короткая), а не 3+ из-за частей-продолжений.

маркертабл #колво-таблиц маркертабл

= Раздел

#таблица(table(
  columns: 2,
  [N], [Значение],
  ..([Строка], [данные] * 1).flatten() * 60,
))[Очень длинная таблица с продолжением]

#таблица(table(
  columns: 2,
  [A], [B],
  [1], [2],
))[Короткая вторая таблица]
