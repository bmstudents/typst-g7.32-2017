#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

// Вставной лист с (в-нумерации: нет) не должен учитываться в сквозной
// нумерации: страницы ПОСЛЕ него продолжают счёт так, будто листа не было.
//
// Раскладка:
//   стр.1  — основной текст (footer "1")
//   вставной лист (в-нумерации: нет) — НЕ занимает номер
//   стр.2  — текст после вставки (footer должен быть "2", а не "3")
//   стр.3  — ещё текст (footer "3")

= Первый раздел
#lorem(30)

#вставить-лист(rect(width: 10cm, height: 10cm, [СКАН ЗАДАНИЯ]), в-нумерации: нет)

#pagebreak()
= Второй раздел
#lorem(30)

#pagebreak()
= Третий раздел
#lorem(30)
