#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

= Раздел
// Смешанный кириллица+латиница + одинаковое начало ключа («Кэш»/«Кэширование»).
#определение("r1", "Яблоко — плод")
#определение("r2", "Apple — английское яблоко")
#определение("r3", "Банан — тропический плод")
#определение("r4", "Zebra — полосатое животное")
#определение("r5", "Арбуз — бахчевая ягода")
#определение("r6", "Кэширование — процесс сохранения копий")
#определение("r7", "Кэш — буфер быстрого доступа")
#определение("r8", "Маска — двоичный шаблон")
#определение("r9", "Массив — индексируемый набор")
#определение("r10", "Mid — серединное значение")

#определения_обозначения_сокращения_раздел()
