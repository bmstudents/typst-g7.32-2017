#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

= Раздел

// Таблица сразу после таблицы (без текста между). Обе обязаны быть
// пронумерованы по-разному (1 и 2), без слипания и без «Продолжение».
#таблица(table(
  columns: 2,
  [перваяА], [перваяБ],
))[Первая подряд]
#таблица(table(
  columns: 2,
  [втораяА], [втораяБ],
))[Вторая подряд]
