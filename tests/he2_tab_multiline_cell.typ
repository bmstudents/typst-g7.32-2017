#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

= Раздел

// Многострочная ячейка (явный перенос через \). Соседняя короткая ячейка
// должна выровняться по верху, высота строки растёт, подпись над таблицей.
#таблица(table(
  columns: 2,
  [ВЕРХОДИН \ СЕРЕДИНА \ НИЗТРИ], [короткаяячейка],
))[Многострочная ячейка]
