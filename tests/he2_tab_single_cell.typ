#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

= Раздел

// Таблица из ОДНОЙ ячейки (1 колонка, 1 строка). Проверяем: подпись
// «Таблица 1 – ...» появляется один раз, нет ложного «Продолжение
// таблицы», левое выравнивание подписи под левой границей таблицы.
#таблица(table(
  columns: 1,
  [ЕДИНСТВЕННАЯ],
))[Одна ячейка]
