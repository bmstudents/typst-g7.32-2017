#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

= Раздел

// Спецсимволы/юникод в ячейках: ± × № — и числовые столбцы.
// Проверяем сохранность символов и выравнивание числового столбца.
#таблица(table(
  columns: 3,
  [№], [Параметр], [Значение],
  [1], [Допуск], [±5],
  [2], [Размер], [8 × 8],
  [3], [Диапазон], [10 — 20],
))[Спецсимволы №±×—]
