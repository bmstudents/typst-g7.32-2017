#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

= Раздел

Текст.

// Рисунок и длинная таблица оба прижаты к нижнему полю. Рисунок —
// неразрывный блок (тело+подпись), переезжает целиком; таблица сразу за
// ним переносится с «Продолжение».
#v(480pt)

#рис(rect(width: 10cm, height: 8cm))[Рисунок схемы устройства]

#таблица(table(columns: 2,
    ..range(1, 30).map(i => ([стр#i], [зн#i])).flatten()
))[Таблица данных у нижнего поля]

Хвост документа.
