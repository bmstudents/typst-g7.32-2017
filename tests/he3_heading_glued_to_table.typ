#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

= Раздел

Текст.

// Заголовок подраздела выталкивается к самому нижнему полю; таблица идёт
// сразу под ним. Заголовок (sticky-блок) не должен оторваться от таблицы:
// либо оба внизу страницы, либо оба переезжают на следующую вместе.
#v(700pt)

== Подраздел перед таблицей

#таблица(table(columns: 2,
    [A], [B], [1], [2], [3], [4]
))[Таблица сразу под заголовком]

Хвостовой текст.
