#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

= Раздел

// 80 строк данных: таблица гарантированно перетекает на 3+ страницы.
#таблица(table(
    columns: (2cm, 1fr),
    [Ключ], [Значение],
    ..range(1, 81).map(i => ([К#i], [В#i])).flatten()
))[Очень длинная таблица из восьмидесяти строк]

Текст после таблицы.
