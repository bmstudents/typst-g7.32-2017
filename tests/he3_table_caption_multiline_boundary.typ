#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

= Раздел

Текст перед таблицей.

// Подпись таблицы многострочная и таблица вытолкнута к нижнему полю.
// Многострочная подпись (она же table.header) должна целиком держаться с
// первой строкой тела — не остаётся «висячая» часть подписи на стр.1.
#v(640pt)

#таблица(table(columns: 2,
    [N], [Значение],
    ..range(1, 8).map(i => ([стр#i], [знач#i])).flatten()
))[Очень длинная многострочная подпись таблицы, которая занимает несколько строк текста и должна держаться вместе с первой строкой тела таблицы на одной странице, не оставляя сироту наверху предыдущей]

Текст после таблицы.
