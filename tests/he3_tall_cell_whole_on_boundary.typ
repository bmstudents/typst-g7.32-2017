#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

= Раздел

Текст перед таблицей.

// Выталкиваем таблицу почти к нижнему полю: высокая НЕРАЗРЫВНАЯ ячейка
// (ячейка[] = block(breakable:false)) не должна резаться посреди строки
// на границе — она целиком переезжает на следующую страницу с шапкой
// «Продолжение».
#v(640pt)

#таблица(table(
    columns: (2cm, 1fr),
    [N], [Описание],
    [1], [Короткая строка.],
    [2], ячейка[Строка один. \ Строка два. \ Строка три. \
        Строка четыре. \ Строка пять. \ Строка шесть.],
    [3], [Ещё строка после высокой.],
))[Граничная таблица с высокой ячейкой]

Текст после таблицы.
