#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

#реферат
Отчёт о НИР. РИС #колво-рисунков РИС, ТАБ #колво-таблиц ТАБ,
ИСТ #колво-источников ИСТ, ПРИЛ #колво-приложений ПРИЛ.
Ключевые слова: микроконтроллер, алгоритм, испытания.

#содержание()

#введение
Введение описывает актуальность и задачи. Источник @kuznetsov2021.

= Аналитический раздел
Обзор и постановка задачи.
== Существующие решения
Анализ аналогов по работе @morozova2018.
#рис(rect(width: 5cm, height: 3cm))[Структурная схема] <fa1>
== Требования
Требования к системе.
#таблица(table(columns: 2, [Параметр], [Значение], [Частота], [16]))[Требования] <ta1>
$ f = 1 / T $ <ea1>

= Проектный раздел
Разработка системы.
== Алгоритм
Описание алгоритма работы.
#листинг(```python
def main():
    init()
    while True:
        step()
```)[Главный цикл] <la1>
#рис(rect(width: 5cm, height: 3cm))[Блок-схема алгоритма] <fa2>
== Расчёты
Расчётная часть.
$ E = (m v^2) / 2 $ <ea2>
#таблица(table(columns: 2, [Режим], [Ток], [Сон], [2]))[Режимы работы] <ta2>

= ЗАКЛЮЧЕНИЕ
Ссылки на @fa1, @ta1, @ea1, @fa2 подтверждают результаты.

#bibliography("he4_refs.yml")

#show: приложение.with(буква: "А", содержание: [Дополнительные схемы])
Материалы приложения А.
#рис(rect(width: 5cm, height: 3cm))[Схема приложения]
#таблица(table(columns: 1, [данные]))[Таблица приложения]

#show: приложение.with(буква: "Б", содержание: [Листинги])
Материалы приложения Б.
#листинг(```c
int sum(int a, int b) { return a + b; }
```)[Функция суммирования]
