#import "../gost732-2017/g7.32-2017.typ": *

// да/нет — это булевы константы пакета: проверяем их прямую семантику.
да == true и нет == false: #(да == true and нет == false).

#show: гост732-2017.with(
  фича-маленький-отступ-вокруг-таблиц: нет,
  фича-переносы-слов: да,
  фича-отступ-перед-подразделом: нет,
  фича-неразрывные-величины: нет,
)

Все четыре флага приняли значения да/нет одновременно.
