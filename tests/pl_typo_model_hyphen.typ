#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017.with(фича-неразрывные-величины: да)

// Документация пакета (styles/typography.typ) обещает: «названия моделей
// (L298N, ESP32-S2, WS2812B) не рвутся внутри по дефису».
// Box 56pt: модель 'ESP32-S2' целиком (~55pt) помещается на отдельной
// строке, 'яяя' уходит выше. Проверяем, что дефис ВНУТРИ модели не стал
// точкой переноса.
#box(width: 56pt)[
  яяя ESP32-S2
]
