#import "../gost732-2017/g7.32-2017.typ": *
#show: гост732-2017

= Раздел

Текст перед таблицей.

#v(560pt)

#таблица(
    table(
        columns: (4cm, 1fr),
        [№], [Описание],
        [1], [Короткая строка.],
        [2], ячейка[Высокая ячейка с многострочным текстом: строка один, \
            строка два, строка три, строка четыре, строка пять, \
            строка шесть — всего около шести строк подряд, которые \
            должны держаться вместе и не рваться посреди ячейки на \
            границе страницы криво, а перенестись целиком с шапкой \
            «Продолжение таблицы».],
        [3], [Ещё строка после высокой.],
    )
)[Граничная таблица]

Текст после таблицы.
